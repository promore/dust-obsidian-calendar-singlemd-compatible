# Dust Calendar (fork)

本仓库是 [Dust Calendar](https://github.com/a-nano-dust/dust-obsidian-calendar) 的一个 fork 版本，基于原项目做了针对性的功能扩展。

原项目地址：<https://github.com/a-nano-dust/dust-obsidian-calendar>

## 本 fork 修改的内容

### 1. 天格子待办标注支持月度笔记文件

原项目中，待办标注（颜色 / 圆孔）只能从「每日笔记」文件（如 `yyyy-MM-dd.md`）读取，天格子无法标注待办。

本 fork 扩展为：当每日笔记文件不存在时，自动回退到「每月笔记」文件（如 `yyyy-MM.md`），并读取其中对应日期小节（`## yyyy-MM-dd`）下的待办事项来标注天格子。

- 若每日笔记文件存在，仍按每日笔记文件统计（保持原行为）；
- 若每日笔记文件不存在，则从月度笔记文件中对应日期的 `## yyyy-MM-dd` 小节统计待办并标注到天格子。

这适用于将每天事件统一存放在月度文件（`# yyyy-MM` 一级标题 + `## yyyy-MM-dd` 二级标题）中的使用方式。

### 2. 修复构建类型错误

修复了 `src/core/NoteController.ts` 中误用全局变量 `app`、以及 `src/main.ts` 中 `leaf` 可能为 `null` 的类型错误，使 `pnpm run build` 能够通过完整的 TypeScript 类型检查。

### 3. 可配置的节假日修正数据

`lunar-typescript` 的官方默认节假日数据存在更新及时性问题，2026 年及以后的节假日标注与调休信息可能不准确，且原插件无任何途径让用户修正。

本 fork 新增「节假日修正数据」配置功能，允许用户按年份手动指定 `HolidayUtil.fix` 数据，在插件加载时应用，从而修正官方默认节假日与调休信息。

#### 功能说明

- **按年份配置**：设置界面「节假日修正数据」区域支持为 2025~2099 范围内的任意年份配置修正数据字符串。
- **默认内置 2026 年数据**：插件内置了 2026 年全年节假日与调休修正数据，开箱即用。
- **加载时应用**：插件加载时遍历所有已配置年份，调用 `HolidayUtil.fix(year, data)` 应用修正；空数据跳过、单个年份数据异常不会阻塞加载。
- **持久化**：配置数据随插件设置保存到 `data.json`。

#### 如何获取最新年份数据

1. 前往 <https://6tail.cn/calendar/api.html#holiday-util.fix.html>（lunar 日历官网的 HolidayUtil.fix 接口文档）。
2. 从页面获取目标年份的节假日修正字符串（`HolidayUtil.fix("2027", "...")` 中的字符串参数）。
3. 在插件设置 →「节假日修正数据」中点击「新增年份」输入年份，将字符串粘贴到对应年份的输入框中。

#### 界面布局

「节假日修正数据」配置区采用三行布局：

- **第一行**：左侧为「节假日修正数据」标题，右侧为「年份输入框 + 新增年份」按钮。
- **第二行**：功能说明与官网获取链接。
- **第三行**：已配置年份的修正数据字符串列表，每个年份一行（年份标签 + 输入框 + 删除按钮）。

## 使用说明

请参考原项目文档：<https://github.com/a-nano-dust/dust-obsidian-calendar>
